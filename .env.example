# 《禁闭逃杀》v1.0 环境变量
#
# 不填任何变量 → 项目自动以「单机/本地兜底」模式运行：
#   房间数据存在浏览器 localStorage，仅支持同一浏览器多标签页热座测试。
#
# 填好下面三个变量 → 项目以「真实多人」模式运行：
#   房间数据存到 Supabase Postgres，任何设备打开链接即可加入、刷新可重连。
#
# 获取方式：登录 https://supabase.com → 新建项目 → Project Settings → API。

# 客户端可见（决定前端走「远程」还是「本地」模式）。填 Project URL。
NEXT_PUBLIC_SUPABASE_URL=

# 客户端可见的匿名公钥（anon public key）。当前服务端 API 实际使用 service role，
# 此变量保留以备将来直连，可留空或填 anon key。
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# 仅服务端使用，切勿暴露给前端。Project Settings → API → service_role secret。
SUPABASE_SERVICE_ROLE_KEY=
